import { While } from "../assets/While.js";

// STEP 4

export class Assembler extends While{

    constructor(){
        super()
    }

    main(item, i){

        if(item.type === 'symbol'){

            if(item.name === 'class'){
                this.array[i+1]
            }

        }

    }

}